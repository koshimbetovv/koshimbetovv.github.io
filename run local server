If the page uses JavaScript modules, fetch calls, routing, or assets that behave better through a server, run a tiny local server instead:

python3 -m http.server 8000


Then open:
http://localhost:8000
